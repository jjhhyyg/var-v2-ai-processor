# Windows desktop CUDA worker dependencies.
# Detection is handled by the C++ ONNX CUDA sidecar. Run
# `npm run desktop:export-onnx` from frontend/ when best.pt needs conversion.

opencv-python>=4.10.0
numpy>=2.0.0
python-dotenv>=1.1.0
