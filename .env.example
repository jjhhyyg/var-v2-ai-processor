# AI处理模块环境变量配置示例
# 复制此文件为 .env 并修改相应的配置值

# ==================== Flask服务配置 ====================
AI_PROCESSOR_HOST=0.0.0.0
AI_PROCESSOR_PORT=5000
AI_PROCESSOR_DEBUG=False

# ==================== 后端回调配置 ====================
# 后端服务基础URL
AI_CALLBACK_URL=http://localhost:8080/api/tasks

# ==================== YOLO模型配置 ====================
# YOLO模型路径（相对路径或绝对路径）
# 推荐：将训练好的模型放在 weights/best.pt
YOLO_MODEL_PATH=weights/best.pt
# YOLO模型版本
YOLO_MODEL_VERSION=yolo11n
# 计算设备（空字符串表示自动选择，可选: cpu, cuda, mps, 0, 1等）
YOLO_DEVICE=
# 是否显示详细输出（调试时启用）
YOLO_VERBOSE=False

# ==================== 检测配置 ====================
# 默认置信度阈值（0.1-0.9）
DEFAULT_CONFIDENCE_THRESHOLD=0.5
# 默认IoU阈值（0.3-0.7）
DEFAULT_IOU_THRESHOLD=0.45

# ==================== ByteTrack追踪器配置 ====================
# 追踪器配置文件路径（相对于ai-processor目录）
TRACKER_CONFIG=bytetrack.yaml

# 以下参数仅在使用代码动态配置tracker时生效
# 高置信度阈值（第一次关联）
TRACK_HIGH_THRESH=0.5
# 低置信度阈值（第二次关联）
TRACK_LOW_THRESH=0.1
# 新轨迹初始化阈值
NEW_TRACK_THRESH=0.6
# 轨迹缓冲帧数（目标丢失后保留的帧数）
TRACK_BUFFER=30
# 轨迹匹配阈值（IoU）
MATCH_THRESH=0.8
# 融合检测置信度分数
FUSE_SCORE=False
# 全局运动补偿方法（None/sparseOptFlow/orb/sift/ecc）
GMC_METHOD=None

# ==================== 进度更新配置 ====================
# 进度更新频率（每处理多少帧更新一次）
PROGRESS_UPDATE_INTERVAL=30

# ==================== 存储路径配置 ====================
# 存储基础路径（用于访问视频文件）
STORAGE_BASE_PATH=../storage

# ==================== RabbitMQ配置 ====================
# RabbitMQ服务器地址
RABBITMQ_HOST=localhost
# RabbitMQ端口
RABBITMQ_PORT=5672
# RabbitMQ用户名
RABBITMQ_USER=var_user
# RabbitMQ密码
RABBITMQ_PASSWORD=your_rabbitmq_password_here
# 任务队列名称
RABBITMQ_QUEUE=video_analysis_queue
